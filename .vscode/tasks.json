{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -g -std=gnu++11 Main.cpp SandPile.cpp Point.cpp ScreenBuffer.cpp -pthread -lSDL2main -lSDL2 -o a.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}